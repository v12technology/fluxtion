name: Release to maven central

on:
  push:
    branches: [ release ]

jobs:
  release:
      
    runs-on: ubuntu-latest

    steps:
      - name: Install gpg secret key
        run: |
          cat <(echo -e "${{ secrets.OSSRH_GPG_SECRET_KEY }}") | gpg --batch --import
          gpg --list-secret-keys --keyid-format LONG
          
      - uses: actions/checkout@v2
      - name: Setup java 1.8
        uses: actions/setup-java@v1
        with:
          java-version: 1.8
          server-id: ossrh
          server-username: MAVEN_USERNAME
          server-password: MAVEN_PASSWORD
          
      - name: Cache the Maven packages to speed up build
        uses: actions/cache@v1
        with:
          path: ~/.m2
          key: ${{ runner.os }}-m2-${{ hashFiles('**/pom.xml') }}
          restore-keys: ${{ runner.os }}-m2  
          
      - name: Build project with Maven
        run: |
            git checkout develop
            pwd
            ls
            git status
            mvn --batch-mode  external.atlassian.jgitflow:jgitflow-maven-plugin:1.0-m5.1:release-start -DpushReleases=false -Dfluxtion.github.user=${{ secrets.FLUXTION_GITHUB_USER }} -Dfluxtion.github.password=${{ secrets.FLUXTION_GITHUB_TOKEN }} &&
            mvn --batch-mode  external.atlassian.jgitflow:jgitflow-maven-plugin:1.0-m5.1:release-finish -Prelease -Dmaven.javadoc.failOnError=false -DpushReleases=false -Dgpg.passphrase=${{ secrets.OSSRH_GPG_SECRET_KEY_PASSWORD }} -Dfluxtion.github.user=${{ secrets.FLUXTION_GITHUB_USER }} -Dfluxtion.github.password=${{ secrets.FLUXTION_GITHUB_TOKEN }} &&		
            git push --all && 
            git push --tags
        env:
          MAVEN_USERNAME: ${{ secrets.OSSRH_USERNAME }}
          MAVEN_PASSWORD: ${{ secrets.OSSRH_TOKEN }}